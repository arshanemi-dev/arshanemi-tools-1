import FileExplorer from '@/components/files/FileExplorer'

export const metadata = {
  title: 'Files — MultiImageLink Generator',
}

export default function FilesPage() {
  return <FileExplorer path={[]} />
}
